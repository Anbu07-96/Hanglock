[package]
name = "hanglock-platform"
description = "The seam between Hanglock's model and an operating system. Traits only: no implementation, no platform types."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
hanglock-core = { path = "../hanglock-core" }

[lints]
workspace = true
