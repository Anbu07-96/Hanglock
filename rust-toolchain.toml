[toolchain]
# Pinned so CI, the maintainer and a stranger from GitHub agree on one compiler. Channel is the
# oldest release the code is written against; anything newer builds it unchanged.
channel = "1.77.2"
components = ["rustfmt", "clippy"]
profiles = ["minimal"]
